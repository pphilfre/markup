{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the Markup desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:default"
  ]
}
