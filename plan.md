Allow for local file sync with tauri, I should be able to choose a folder, to save all my files in (whilst still creating them online), it should auto scan the directory for .md .canvas and .mindmap files.  It should read the files that are in the local directory and allow for the option to sync them online, and for them to work locally
Change the URL's to match markup.freddiephilpot.dev as when you try to share a file it redirects to tauri.localhost
Allow for app zooming, like i can do ctrl+ to zoom in the whole app, as i find some ui elements too small on the desktop
Within the app, any download feature should open a folder pick when I want to download something
Make sure all data is saved properly, I dont want any data loss
When I close tabs at the top, they reappear moments after? they should stay disappeared until I click back on them in the file tree
Remove the word FILES on the work tree so we can have more room for the icon
Move the workspace selector to the bottom of the sidebar