Convex sync is not working for the tauri app (but it is on the website), neither is workos widget components, here;s what i want it to do
Im pretty sure its an underlying issue with Tauri, that convex and workos do not work, try using rust to rewrite it. I've heard someone else say its because it's https but im not sure, and a rust rewrite would work better, just make sure the original website will still work!
Fix workos widgets still dont work,its stuck on unable, and the open in browser feature does nothing
add some debug notifcation on the app launch, e.g. cannot connect to convex /workos, or any errors that happen in the code
Use the logo.svg as the tauri logos across devices, use the tauri logo converter to cover all devices
